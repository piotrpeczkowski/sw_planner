import 'package:flutter/material.dart';

class ActualTasksContent extends StatelessWidget {
  const ActualTasksContent({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return ListView(
      children: [],
    );
  }
}
